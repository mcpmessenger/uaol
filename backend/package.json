{
  "name": "uaol-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Universal AI Orchestration Layer - Backend Microservices",
  "type": "module",
  "workspaces": [
    "services/*",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n \"api-gateway,auth,tool-registry,job-orch,tool-proxy,billing,storage\" -c \"cyan,magenta,green,yellow,blue,red,white\" \"npm run dev --workspace=@uaol/api-gateway\" \"npm run dev --workspace=@uaol/auth-service\" \"npm run dev --workspace=@uaol/tool-registry-service\" \"npm run dev --workspace=@uaol/job-orchestration-service\" \"npm run dev --workspace=@uaol/tool-proxy-service\" \"npm run dev --workspace=@uaol/billing-service\" \"npm run dev --workspace=@uaol/storage-service\"",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "eslint . --ext .ts",
    "migrate": "tsx shared/database/migrate.ts",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.54.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@types/multer": "^2.0.0",
    "dotenv": "^16.3.1",
    "glob": "^13.0.0",
    "multer": "^2.0.2"
  }
}
