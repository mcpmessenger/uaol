{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "."
  },
  "include": ["**/*.ts"],
  // Explicitly list critical models so the build never skips them
  "files": [
    "database/models/shareable-link.ts"
  ],
  "exclude": ["node_modules", "dist"]
}

