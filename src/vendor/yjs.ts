// Lightweight re-export of Yjs via remote ESM build to avoid local install issues.
// For production, you can swap this to a pinned local dependency if desired.
export * from 'https://esm.sh/yjs@13.6.15?dev';
